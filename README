mkdir homebrew && curl -L https://github.com/mxcl/homebrew/tarball/master | tar xz --strip 1 -C homebrew

brew install git tig ctags curl readline coreuilts proctools wget cmake gnutls libtasn1 ossp-uuid qt weechat libgcrypt lrzxz pidof libgpg-error pkg-config tree xz

git clone https://github.com/kensei/dotfiles.git
cd dotfiles
./setup.sh

cd ~/.vim
mkdir bundle
cd bundle
git clone https://github.com/gmarik/vundle.git

vim → BundleInstall

cd ~/.vim/bundle/vimproc/
make -f make_mac.mak
